# PASM · 项目介绍（纯文字版）

> 本页用文字讲清 **PASM 是什么、能做什么、怎么用、怎么合作**。
> 配合 `README.md`（含效果演示图与完整教学说明）阅读更佳。
> 对应开源教学版代码：`pasm_lite.py`（约 260 行）。

---

## 一、一句话

PASM（Predictive Agent System with Memory）是一个"**非 token 内部循环**"的
认知智能体框架：AI 的"思考"可以不靠生成 token，而靠
**感知 → 工作记忆 → 情景记忆检索 → 世界模型"想象" → 规划 → 行动** 的连续向量循环。

## 二、它回答的问题

今天的大模型靠"预测下一个词"思考——回答你之前，内部要滚动海量 token 的概率。
但人脑里没有 token：骑车躲坑、伸手接杯子，全程没有一个"词语"在脑内滚动。

PASM 用工程实现这条直觉：内部状态全部是连续潜在向量，只有需要与外界交流时才输出动作。
它不追求"读遍世界然后回答"，而是"活在环境里、边活边学、长成自己的样子"。

## 三、七层认知架构（文字版）

1. **感知编码**：环境观测压缩成低维潜在向量（对应感觉皮层；World Models/Dreamer 方向）
2. **工作记忆**：有限槽位暂存当前任务上下文（对应前额叶-顶叶）
3. **记忆系统**：情景记忆（KNN 检索经历）+ 语义记忆（抽象规律）+ 程序记忆（习惯固化）
   （对应海马体/皮层/基底节；双系统理论）
4. **世界模型**：在潜在空间预测"下一步会怎样、给多少奖励"，即"想象引擎"
   （对应皮层柱/默认模式网络；MuZero/TD-MPC 方向）
5. **情绪与动机**：多巴胺 = 奖励预测误差，调制学习率；血清素调制探索/保守
   （对应边缘系统；神经科学经典结论）
6. **性格与发育**：先天性格种子 × 后天经历漂移；可塑性随"年龄"衰减（童年爱学、成年定型）
7. **元认知与全局广播**：自我调节（何时多想/少想）+ 重要事件全脑广播（对应前额叶高级功能）

## 四、实测证明了什么（数字说话，单 seed 观察、诚实标注）

- 情绪系统价值巨大：消融实验中关闭情绪，奖励恶化 11.7、撞墙率 ×2.3
- 性格真的分化：同环境同预算，谨慎型碰撞比冒险型少 31%，冒险型固化习惯最多
- 记忆会成长：情景记忆事件驱动写入，反复成功的行为固化为"习惯"（免规划直出）
- 检索有规模：2 万条记忆启用 ANN 索引后，查询时延恒定约 0.6ms
- 低算力：CPU 毫秒级单步决策，无需 GPU、无需微调重训

## 五、它能做什么、不能做什么（诚实边界）

**能做（它的主场）**：游戏 NPC（会记住玩家、会成长）、虚拟宠物/陪伴养成、
站点客服的"情绪与状态层"、仿真智能体、教学科研沙盒。

**不做（LLM 的主场）**：百科问答、长文写作、复杂代码。PASM 不是 DeepSeek 的替代品，
而是互补品：PASM 管"状态/记忆/情绪/行动"，LLM 管"语言/常识"（System1/System2 双系统，
完整引擎提供 OpenAI 兼容 API 便于协作）。

## 六、这个仓库是什么（PASM-Lite 教学版）

PASM-Lite 把七层思想压缩成一个 **约 260 行、单文件** 的可运行教学实现
（`pasm_lite.py`），保留零 token 认知循环主干：自编码感知、工作记忆、
情景记忆检索、GRU 世界模型、采样规划。

- 运行：`pip install torch && python pasm_lite.py`
- 改着玩：调食物密度 / 检索宽度 / 规划预算，观察行为与记忆变化
- 诚实边界：它是教学骨架不是性能基线；更强的行为表现依赖情绪/性格/元认知等
  生产模块（对照 README 的能力表，Lite 缺什么一目了然）

## 六点五、生态现状（2026-09-04 更新：PASM 已经长成"大脑"）

PASM 目前是三仓库同频的**认知大脑**形态：

- **PASM**（私有核心仓库）＝七层引擎 + 认知执行皮层 + 桌面全源码 + 文档，引擎已至 **v0.4.0**；
- **pasm-qclaw**（公开）＝PASM Studio 桌面产品发行仓库（Releases 安装包 + 更新通道），
  桌面 **v0.27.0「认知大脑版」**：会读你的文件/文件夹、打开应用与路径、写脚本出错自动修复重跑、
  生成 PPT/Word/Excel、开发全栈项目；每次回复前先由认知皮层判断意图与心情、再按话题自动召回
  分层记忆（工作/情景/程序三层）——跨重启也记得上次聊到哪；
- **PASM-Lite**（本仓库，公开）＝零 token 认知**最小教学实现**，定位不变：它是理解 PASM
  核心思想最快的入口，其余两层能力在教学范围之外。

## 六点六、新增：统一引擎接口（Engine API）

本仓库现在多了一件事：Lite **可以当引擎插进任何系统**，而不只是一段演示代码。

- `engine_api.py`（纯标准库、零依赖；与 PASM 核心 `pasm/engine_api.py` 同源镜像）
  定义引擎契约：`info / capabilities / reset_episode / act / learn / snapshot` 六项必需，
  `save / load / freeze_vae / close` 可选。
- `engine.py` 是教学版对该契约的实现（`LiteEngine`），导入即注册——
  `create("pasm-lite")` 就能拿到一个标准引擎；换成 `create("pasm")` 即生产引擎，
  **调用代码一行都不用改**。
- `capabilities()` 诚实申报自己会什么（教学版只覆盖七层里的主干四层），
  `missing_vs()` 能列出相对完整引擎还缺哪些能力——不做"看起来很全"的假象。
- `snapshot()` 固定七个区块（没有的填 `None`），统一看板可无差别读取任意引擎状态。
- 试一下：`python engine.py`（跑通闭环 + 打印接口一致性自检）。

一句话：**接口是 PASM 生态的插座**，Lite 现在有了标准插头。

## 七、部署与合作

- **教学代码**：本仓库即全部（MIT 协议），clone 即跑
- **完整引擎**：七层实现 + OpenAI 兼容 API + 应用实例为私有项目；
  授权 / POC / 定制（NPC、客服情绪层、陪伴养成等）请在本仓库提 issue 留言
- **文档线索**：README（完整图文介绍 + 教学 + 部署）；`index.md`（本页，纯文字速览）

## 八、License

MIT © 2026 arronZheng。欢迎 fork 学习、提 issue 讨论零 token 认知架构方向。
